# Shana Site — Static site with simple backend

Run locally:

1. Install dependencies:

```bash
npm install
```

2. Start the server:

```bash
npm start
```

3. Open http://localhost:3000 in your browser.

The backend exposes a POST `/api/contact` endpoint which saves messages to `messages.json`.
